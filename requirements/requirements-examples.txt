confluent-kafka
fastapi
pydantic
uvicorn
six

attrs
cachetools
httpx>=0.26
authlib>=1.0.0

fastavro >= 1.5.4, < 1.8.0; python_version == "3.7"
fastavro >= 1.5.4, < 2; python_version > "3.7"
requests
avro>=1.11.1,<2

jsonschema >= 4.18.0

googleapis-common-protos
protobuf

azure-identity
azure-keyvault-keys
boto3
cel-python>=0.4.0
google-auth
google-api-core
google-cloud-kms
hkdf==0.0.3
hvac
jsonata-python
# Dependency of cel-python.  Use version 6 due to https://github.com/yaml/pyyaml/issues/601
pyyaml>=6.0.0
# Request the gcpkms extra rather than listing its contents: tink 1.16.0 added
# google-crc32c/pyasn1/pyasn1-modules to that extra, and a hand-maintained copy of
# the list silently goes stale (the gcpkms import then fails at import time).
tink[gcpkms]
